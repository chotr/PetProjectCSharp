<!DOCTYPE html>
<html lang="zxx" class="no-js">

<head>
    <!-- Mobile Specific Meta -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Favicon-->
    <link rel="shortcut icon" href="~/img/fav.png">
    <!-- Author Meta -->
    <meta name="author" content="CodePixar">
    <!-- Meta Description -->
    <meta name="description" content="">
    <!-- Meta Keyword -->
    <meta name="keywords" content="">
    <!-- meta character set -->
    <meta charset="UTF-8">
    <!-- Site Title -->
    <title>Pets Shop</title>
    <!--
        CSS
        ============================================= -->
    <link href="https://fonts.googleapis.com/css?family=Mulish:300,400,500,600,700,800,900" rel="stylesheet" type="text/css" media="all">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <link rel="stylesheet" href="~/Content/linearicons.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-1ycn6IcaQQ40/MKBW2W4Rhis/DbILU74C1vSrLJxCq57o941Ym01SwNsOMqvEBFlcgUa6xLiPY/NS5R+E6ztJQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="~/Content/themify-icons.css">
    <link rel="stylesheet" href="~/Content/bootstrap.css">
    <link rel="stylesheet" href="~/Content/owl.carousel.css">
    <link rel="stylesheet" href="~/Content/nice-select.css">
    <link rel="stylesheet" href="~/Content/nouislider.min.css">
    <link rel="stylesheet" href="~/Content/ion.rangeSlider.css" />
    <link rel="stylesheet" href="~/Content/ion.rangeSlider.skinFlat.css" />
    <link rel="stylesheet" href="~/Content/magnific-popup.css">
    <link rel="stylesheet" href="~/Content/main.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>

<body>

    <!-- Start Header Area -->
    @Html.Action("MenuPartial", "Home")
    <!-- End Header Area -->
    <!-- Start Banner Area -->
    <section class="banner-area organic-breadcrumb">
        <div class="container">
            <div class="breadcrumb-banner d-flex flex-wrap align-items-center justify-content-end">
                <div class="col-first">
                    <h1>Login/Register</h1>
                    <nav class="d-flex align-items-center">
                        <a href="index.html">Home<span class="lnr lnr-arrow-right"></span></a>
                        <a href="category.html">Login/Register</a>
                    </nav>
                </div>
            </div>
        </div>
    </section>
    <!-- End Banner Area -->
    <!--================Login Box Area =================-->


    <section class="login_box_area section_gap_bottom">
        @RenderBody()
    </section>
    <!--================End Login Box Area =================-->
    <!-- start footer Area -->
    <footer>
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-3">
                    <div class="logo_footer">
                        <a href="@Url.Action("Index", "Home")">
                            <img src="~/img/icon/logo.png" />
                        </a>
                    </div>
                </div>
                <div class="col-md-3">
                    <h2>Menu</h2>
                    <ul>
                        <li><a href="@Url.Action("Index", "Home")">Home</a></li>
                        <li><a href="@Url.Action("SanPham", "SanPham")">Product</a></li>
                        <li><a href="@Url.Action("About", "Home")">About</a></li>
                        <li><a href="@Url.Action("Contact", "Home")">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h2>Contact</h2>
                    <ul>
                        <li><a href="@Url.Action("Contact", "Home")">principal office: Hamlet 6, Linh Trung Ward, Thu Duc District, Ho Chi Minh City,</a></li>
                        <li><a href="#">phone: 1800 1211</a></li>
                        <li><a href="#">email: gallmount1899@gmail.com</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h2>connect to us</h2>
                    <p class="social">
                        <a href="https://www.facebook.com/nguyenson.nguyen.12576"><i class="fa fa-facebook-square"></i></a>
                        <a href="#"><i class="fa fa-twitter-square"></i></a>
                        <a href="#"><i class="fa fa-google-plus-square"></i></a>
                        <a href="#"><i class="fa fa-linkedin-square"></i></a>
                    </p>
                </div>
                <hr>
                <div class="col-md-12 text-center">
                    <p>Group 9 .NET. All Rights Reserved</p>
                </div>

            </div>
        </div>

    </footer>
    <!-- End footer Area -->

    <script src="~/Scripts/vendor/jquery-2.2.4.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.11.0/umd/popper.min.js" integrity="sha384-b/U6ypiBEHpOf/4+1nzFpr53nxSS+GLCkfwBdFNTxtclqqenISfwAzpKaMNFNmj4"
            crossorigin="anonymous"></script>
    <script src="~/Scripts/vendor/bootstrap.min.js"></script>
    <script src="~/Scripts/jquery.ajaxchimp.min.js"></script>
    <script src="~/Scripts/jquery.nice-select.min.js"></script>
    <script src="~/Scripts/jquery.sticky.js"></script>
    <script src="~/Scripts/nouislider.min.js"></script>
    <script src="~/Scripts/countdown.js"></script>
    <script src="~/Scripts/jquery.magnific-popup.min.js"></script>
    <script src="~/Scripts/owl.carousel.min.js"></script>
    <!--gmaps Js-->
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCjCGmQ0Uq4exrzdcL6rvxywDDOvfAu6eE"></script>
    <script src="~/Scripts/gmaps.min.js"></script>
    <script src="~/Scripts/main.js"></script>

    <script>

        function validateBeforeSigin() {
            var countError = 0;
            var username = $('#username').val();
            var password = $('#password').val();

            if (username == "") {
                $('#errorUsername').css('color', 'red');
                $('#errorUsername').text('Please input username');
                countError++;

            }
            if (password == "") {
                $('#errorPassword').css('color', 'red');
                $('#errorPassword').text('Please input password');
                countError++;
            }
            if (countError == 0) {
                return true;
            }
            return false;
        }


        function checkValidation() {
            var countErr = 0;
            var fullname = $('#fullname').val();
            var birthday = $('#birthday').val();
            var phone = $('#phone').val();
            var email = $('#email').val();
            var address = $('#address').val();
            var username = $('#username').val();
            var password = $('#password').val();
            var repassword = $('#repassword').val();

            if (fullname.length == "") {
                showErrorEmpty('errFullName');
                countErr++;
            }


               if (birthday.length == "") {
                showErrorEmpty('errBirthday');
                countErr++;
            }
            if (phone.length == "") {
                showErrorEmpty('errPhone');
                countErr++;
            }
            if (email.length == "") {
                showErrorEmpty('errEmail');
                countErr++;
            }

            if (address.length == "") {
                showErrorEmpty('errAddress');
                countErr++;
            }
            if (username.length == "") {
                $('#errorUserName').css('color', 'red');
                $('#errorUserName').text('Account name must be at least 5 characters!');
                countErr++;
            }
            if (password.length < 6) {
                $("#errPassword").css('color', 'red');
                $("#errPassword").text('Minimum 6 characters password!');
                countErr++;
            }
            if (repassword.length < 6) {
                $("#errRepassword").css('color', 'red');
                $("#errRepassword").text('Minimum 6 characters password!');
                countErr++;
            }
            if ((password.length >= 6 && password != repassword && repassword.length >= 6) ){
                countErr++;
                $("#errRepassword").css('color', 'red');
                $("#errRepassword").text('Password incorrect!');
            }

            console.log(countErr);
            if (countErr == 0) {
                return true;
            }

            return false;





        }



        function showErrorEmpty(idEle2) {

            $("#" + idEle2 + "").css('color', 'red');
            $("#" + idEle2 + "").text('Can not be empty!');
        }
        function removeError(idEle) {
            $("#" + idEle+"").remove();
        }

        $(document).ready(function () {



            $('#fullname').on('keyup', function () {
                if ($('#fullname').val() != "") {

                    removeError('errFullName');
                }
            });

            $('#birthday').on('change', function () {
                if ($('#birthday').val() != "") {
                    removeError('errBirthday');

                }
            });

            $('#phone').on('keyup', function () {
                if ($('#phone').val() != "") {
                    removeError('errPhone');

                }
            });


            $('#email').on('keyup', function () {
                if ($('#email').val() != "") {
                    removeError('errEmail');

                }
            });

            $('#address').on('keyup', function () {
                if ($('#address').val() != "") {
                    removeError('errAddress');

                }
            });

            $('#username').on('keyup', function () {
                if ($('#username').val() != "") {
                    removeError('errAddress');

                }
            });



            $('#password, #repassword').on('keyup', function () {

                console.log($('#password').val().length);

                if ($('#password').val().length < 6) {

                    $("#errPassword").css('color', 'red');
                    $("#errPassword").text('Minimum 6 characters password!');

                } else {
                    removeError('errPassword');
                    if (($('#password').val() == $('#repassword').val())) {
                        removeError('errRepassword');

                    } else {
                        $("#errRepassword").css('color', 'red');
                        $("#errRepassword").text('Password incorrect!');
                    }
                }




            })
        }
        );


        $('#username').on('keyup', function () {

            var username = $('#username').val();
            if (username.length < 5) {

                $('#errorUserName').css('color', 'red');
                $('#errorUserName').text('Account name must be at least 5 characters!');

            } else {
                $.ajax({
                    dataType: 'json',
                    url: '@Url.Action("checkUserName", "Register")',
                    type: 'GET',
                    contentType: 'application/json; charset=utf-8',
                    data: {
                        username: username,
                    },
                    success: function (data) {

                        if (data == "exist") {

                            $('#errorUserName').css('color', 'red');
                            $('#errorUserName').text('This account already exists!');
                        } else {

                            $('#errorUserName').css('color', 'green');
                            $('#errorUserName').text('Usable account');
                        }
                    }




                })
            }


        });


        $(document).ready(function () {
            $('#username').on('keyup', function () {
                if ($('#username').val() != "") {

                    $('#errorUsername').text('');
                }
            })
            $('#password').on('keyup', function () {
                if ($('#password').val() != "") {
                    $('#errorPassword').text('');


                }
            })


        });
        function validation() {
            var otp = $('#otpcode').val();
            if (otp == "") {
                $('#errorOTP').css('color', 'red');
                $('#errorOTP').text('Please input OTP from your mail');
                return false;
            }
            return true;
        }
        $(document).ready(function () {
            $('#otpcode').on('keyup', function () {
                if ($('#otpcode').val() != "") {
                    $('#errorOTP').text('');
                }
            })
        });

        function validationChangePass() {
            var countErr = 0;
            var pass = $('#newpass').val();
            var repass = $('#renewpass').val();
            var countTrue = 0;
            if (pass == "" || pass.length < 6) {
                $('#errorNewpass').css('color', 'red');
                $('#errorNewpass').text('Password password has at least 6 characters');
                countErr++;
            } else {
                countTrue++;

            }
            if (repass == "" || repass.length < 6) {
                $('#errorRenewpass').css('color', 'red');
                $('#errorRenewpass').text('Password password has at least 6 characters');
                countErr++;
            } else {
                countTrue++;

            }
            if (countTrue == 2) {
                if ($('#newpass').val() != $('#renewpass').val()) {
                    $('#errorRenewpass').css('color', 'red');
                    $('#errorRenewpass').text('Password and retypepass need same');
                    countErr++;
                }
            }


            if (countErr == 0) {
                return true;
            }
            return false;



        }
        $(document).ready(function () {
            $('#newpass, #renewpass').on('keyup', function () {
                console.log('a');
                if ($('#newpass').val().length < 6) {
                    $('#errorNewpass').css('color', 'red');
                    $('#errorNewpass').text('Password password has at least 6 characters');

                } else {

                    $('#errorNewpass').text('');
                    if ($('#newpass').val() != $('#renewpass').val()) {
                        $('#errorRenewpass').css('color', 'red');
                        $('#errorRenewpass').text('Password and retypepass need same');
                    } else {
                        $('#errorRenewpass').text('');
                    }

                }
            })
        });
        function checkValidationForget() {
            var countError = 0;
            var username = $('#username').val();
            var email = $('#email').val();

            if (username == "") {

                $('#errorUsername').css('color', 'red');
                $('#errorUsername').text('Please input username');
                countError++
            }
            if (email == "") {
                $('#errorEmail').css('color', 'red');
                $('#errorEmail').text('Please input email');
                countError++;

            } else {
                if (!validateEmail(email)) {
                    $('#errorEmail').css('color', 'red');
                    $('#errorEmail').text('Incorrect email format');
                    countError++;
                }
            }
            if (countError == 0) {
                return true;
            }
            return false;
        }

        $(document).ready(function () {

            $('#username').on('keyup', function () {
                if ($('#username').val() != "") {
                    $('#errorUsername').text('');
                }
            })
            $('#email').on('keyup', function () {
                if ($('#email').val() != "") {
                    $('#errorEmail').text('');
                }
            })
        });

    </script>

</body>

</html>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/custom-validator")
}